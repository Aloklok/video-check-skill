# 2026-07-16 视频标注审核技能规则修订

按用户（Alok）要求补两条 speech/字幕 审核原则，消解文档内部冲突：

1. **新增 §7.2.3.6（总表）**：`<speech>` 归属镜头 + 与画面文字对应铁律
   - 多镜头下每个 speech 必须写在真实所属 `<Shot N>` 内（对照 §7.5.9）；
   - speech 听录须与字幕/可见文字对应一致，按 §7.2.7 逐句核对。

2. **新增 §7.2.7.0（总表）+ 重写 SKILL.md note 11、新增 note 12**：字幕逐句绑定 speech 原则
   - 字幕须逐句紧跟对应 `<speech>`（speech A，字幕为A；speech B，字幕为B），**禁止镜头/shot 末尾统一罗列**（如"字幕为A，B，C"）；
   - **废止旧「全部一致→结尾仅一句总结」写法**（原 §7.2.7.1 / 旧 note 11 与此冲突，旧 note 11 已改写，Step 3.5 本就正确故保留并强化）。
   - SKILL.md Step 3 ④⑤ 表格同步加「speech 归属镜头、与字幕对应」及「逐句紧跟、禁止罗列」。

3. 总表版本变更记录新增 **V1.3（2026-07-16）**。

注意：用户明确要求字幕逐句紧跟对应 speech，与旧版"总结句"写法相左，以用户口径为准统一。

## 补充：维护操作约定（2026-07-16 末尾）
- 用户补要求：**以后改本 skill 规则前先 `git pull`**（技能目录为 git 仓库，origin: git@github.com:Aloklok/video-check-skill.git）。
- 已写入 SKILL.md 顶部「维护本 skill 的操作约定」小节，并新建 `.workbuddy/memory/MEMORY.md` 长期记录。
- 本次会话改动（SKILL.md / references/总表）尚未 git commit；本次未执行 pull（避免覆盖未提交改动）。

## 已提交并推送（2026-07-16 01:21 左右）
- `git commit` → 提交号 `979f158`，4 files changed, +58/-13（含 `.workbuddy/memory/*`）。
- 推送前执行 `git pull --rebase`：拉取远程并发提交 `02a4688`，已将本次改动 rebase 其之上，无冲突。
- `git push origin main` 成功 → 远端新提交 `519130c`。
- 后续约定已生效：今后改 SKILL.md/references 前先 `git pull`，改完 commit/push。

## Shot 时间格式修正（2026-07-16 01:25 左右）
- 用户明确 §7.5.1 的 `<shot_N>` 时间戳须为 `x.xs` 形态：**一位小数 + 小写 `s`**（如 `0.0s`、`4.8s`）。
- 总表 §7.5.1 模板：占位符 `Xs`/`Ys` 改为具体 `0.0s`/`4.8s`、`4.8s`/`9.5s`；新增「时间格式铁律」禁止大写 `S`、无小数、两位小数等形态。
- 总表版本变更记录新增 **V1.4（2026-07-16）**。
- 本次改动同样先 `git pull --rebase`（已 up to date），日志文件先 stash 再 pop，工作树无脏。

## ⚠️ 重大发现：Skill 双副本，新规未生效（2026-07-16 01:28）
- 调用 Skill 工具 `视频标注审核` 时，实际 Base directory = `视频标注审核.bak/`（Jun 30 陈旧副本：无 `.git`、无 `references/`、仍是旧「字幕→结尾总结句」规则）。
- 我本次会话编辑的 `视频标注审核/`（git 仓库，含 references/总表 V1.4、字幕逐句绑定/speech归属/x.xs 新规）**并非 Skill 实际加载那份**。即用户定的三条新规则当前未生效。
- 待用户决定：删除 `视频标注审核.bak/` 或让其失效，确保活跃 skill 指向 git 仓库那份（否则改了也白改）。

## 审核样本：动漫短视频（3镜头）（2026-07-16 01:28）
- 样本字段齐全：Event/Role(2)/Background(1)/Music/Style/Scene(3 shot)。
- 主要问题：①Role 的 ID_A 有台词但缺音色/音调/口音；②Shot_1 景别「中近景特写」混搭（须二选一）；③Shot_2 旁白「字幕与台词内容一致」待核实（旁白对应 UI 面板文字=画面可见文字，未必有底部字幕）；④§4.4 动态贴片缺「消失」要素；⑤Shot_3 `<sfx>哈哈哈的笑声</sfx>` 冗余（应为 `<sfx>哈哈哈</sfx>`）。
- 样本正确项：字幕已逐句紧跟各 speech（符合新规则）、时间戳 x.xs 格式正确、shot 结构闭合、Event 无 ID 指代、无分点/推测词。
- 不建议筛去（不满足任何筛去条件）。

## Shot 合并判定（2026-07-16 01:36）
- 用户确认：Shot_1 腰部以上=中景(非特写)；Shot_2 全景→缩放中景(推镜)；Shot_3 实为 Shot_2 缩放后「向左平移(移镜头)」连续运动，非硬切。
- 判定：Shot_2+Shot_3 应合并为同一 shot（依据 §7.5 镜头运动≠多镜头 + §7.5.3 镜头内运镜不新开shot）；原"镜头切换至"措辞错误，应为"镜头向左平移"。合并后共2镜头，时间 3.0s–18.6s，原 0.1s 空隙消失。
- 待用户回看确认平移是否连续无硬切（若无硬切则确为一个 shot）。

## .bak 处置 + 软链接真相（2026-07-16 01:45）
- 排查三处目录：①`~/.workbuddy/skills/视频标注审核/`（git 仓库，真）；②`~/.workbuddy/skills/视频标注审核.bak/`（仅一个 SKILL.md，2026-06-30，非 git、无 references/）；③`/Users/Alok/视频标注/视频标注审核-skill/`（**符号链接**，指向①）。
- **真相**：外部文件夹 `视频标注审核-skill` 本就是指向 git 仓库的 symlink（2026-07-13 建）= 用户想要的"镜像映射"已通过软链接实现（同 inode，外部加文档即改仓库、受 git 跟踪）。所以早先 `cp` 报 identical、外部"空目录"是假象（find 不跟随 symlink）。
- `.bak` 是残留旧备份，导致 Skill 加载器把"视频标注审核"解析到它而非真仓库 → 新规不生效。已将 `.bak` 移出 skills 目录至 `/Users/Alok/视频标注/__bak_removed_视频标注审核`（保留可恢复）。
- **验证**：重新调用 Skill，Base directory 已正确指向 `~/.workbuddy/skills/视频标注审核`，新规则（字幕逐句绑定/§7.2.3.6/x.xs）与 git pull 约定均在加载内容中 → 新规已生效。
- 结论：无需额外"镜像拷贝"，symlink 即活镜像。用户后续在外部文件夹加文档我可直接读，且自动 git 跟踪。

## 审核纪律复盘：主观词漏检（2026-07-16 02:03）
- 用户批评：同一类「神情/主观」词（自傲、愉悦），我第一版审出"自傲"却漏了"愉悦"——**规则清楚（§7.2 边界）但我的跨字段扫描纪律不一致**。且曾自作主张往稿里加源样本没有的东西（"（膝盖以上）""类似玻璃破碎"），被用户纠正"基于我这个改，别臆测"。
- **修复（已入 SKILL.md note 13/14，commit 0871f88）**：①新增「主观/内心词跨字段扫描」强制步骤——Event/Role/Background/Style/Scene 逐字段扫，主观词（自傲/愉悦/无语/不屑等）一律判违规改可见描写；**Music 字段例外**（允许氛围/情绪）；铁律：原稿同样要过此规则、同类词须一次全改不许漏；②新增「景别术语不重复解释」（中景≠膝盖以上 不再括号冗余）。
- 用户明确期望：这类明确规则不该由他逐字段提醒，应由审核流程系统性兜底。

## 审核样本2：写实办公室6人（2026-07-16 02:08）
- 字段齐全：Event/Role(6)/Background(1)/Music/Style/Scene(2 shot)。
- **note 13 生效验证**：跨字段主观词扫描揪出 ① Role ID_A「神情活跃」主观词；② Scene shot_2「惊讶且兴奋的神情」中「兴奋」主观词。两处均已判违规改可见描写（不再漏检，证明 note 13 兜底有效）。
- **note 11 生效**：shot_2 speech「600元」与字幕一致却铺开外观（紫影粗体黄字），判违规改「字幕与台词内容一致」。
- **其他**：ID_A 有台词但 Role 缺声音特征（补"声音清脆，男声"）；ID_F「浅棕色色打底」笔误（"棕色色"重复）；shot_1 字幕「害我也好想玩喔」经用户确认无声音只字幕，保持画面可见文字描述（不补 speech、不触发 note 11 简写），合规。
- **自我纠错**：初判以为 Background 含 `<sfx>` 位置不当，查 §4.3 确认「Background=现场环境音」→ 环境底噪归 Background 合规，纠正误判。
- 不建议筛去。
