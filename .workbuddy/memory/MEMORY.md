# 视频标注审核 Skill - 长期项目笔记

## 维护操作约定
- **改规则前先 `git pull`**：技能目录是 git 仓库（origin: `git@github.com:Aloklok/video-check-skill.git`）。修改 `SKILL.md` / `references/` 前务必先 `git pull`，改完再 `commit` / `push`。用户 Alok 于 2026-07-16 明确要求，已同步写入 SKILL.md 顶部「维护本 skill 的操作约定」小节。
- 规则唯一真源为 `references/视频Caption审核规则总表.md`；SKILL.md 只放流程与注意项，不重复写规则细节。

## 规则版本
- 总表当前 **V1.4（2026-07-16）**：在 V1.3 基础上新增 §7.5.1 Shot 时间戳固定为 `x.xs` 形态（一位小数+小写 s，禁止大写 S/无小数/两位小数）；SKILL.md 同步。

## 改标工作约定（用户 Alok 强约束，2026-07-16）
- **改标必须基于用户提供的版本 / 视频描述，禁止臆测、禁止自行添加源样本里没有的细节**。本次教训：我曾在稿中自加「（膝盖以上）」（按用户一句口述推断）和「类似玻璃破碎的声响」（对 sfx 的修饰），用户明确反对——"谁让你这么写的？不去查镜头？""基于我这个改"。
- 处理方式：用户给出版本就严格照其版本出稿；用户描述不清/规则未覆盖 → 先查总表，仍无据则提问确认，**绝不自补画面外信息**。景别等术语不确定时查总表 §7.4 术语表（如 中景=膝盖以上，勿重复加括号说明）。
